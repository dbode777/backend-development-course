//Generate the array containing every number between a preset min and max (inclusive).

const arr = [];
const min = -11;
const max = 31;

for (i = min; i <= max; i++) {
    arr.push(i);
}

console.log(arr)